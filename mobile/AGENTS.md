# ⚠️ Frozen — superseded by ios/

This client is **frozen as of 2026-07-26**: the native iOS app (`ios/FinanceTracker`) is the
active mobile client going forward. Do not add features, apply performance work (e.g. the
`/portfolio/snapshots/household/{id}` `latest_only`/`/timeseries` split — see root `AGENTS.md`),
or otherwise extend this app unless the user explicitly asks for a change scoped to `mobile/`.
Existing functionality keeps running as-is; this is not a removal, just a stop on new
investment. If asked to un-freeze or remove this client, confirm scope with the user first —
see the removal option that was deliberately not taken when this freeze was decided.

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
