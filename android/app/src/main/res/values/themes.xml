<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Colours and typography all come from the Compose Material 3 theme; this XML theme
      only has to hand the window over edge-to-edge before Compose takes control, so the
      system bars are transparent and Compose draws behind them (see enableEdgeToEdge()).
    -->
    <style name="Theme.Waypoint" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
