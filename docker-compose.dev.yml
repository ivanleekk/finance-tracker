services:
    backend:
        ports:
            - "5001:5001"
        volumes:
            - ./backend:/app
        environment:
            - DEBUG=true

    frontend:
        ports:
            - "5173:5173"
        environment:
            - CHOKIDAR_USEPOLLING=true
        volumes:
            - ./frontend:/app
            - /app/node_modules
      
    db:
        ports:
            - "5432:5432"
